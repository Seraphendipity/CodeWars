#include <stdio.h>

enum DamageType{
   eMin = 0,
   Fire,
   Water,
   Grass,
   Lightning,
   eMax = Lightning
};

enum TypeEffectiveness

int calculate_damage(const char *your_type, const char *opponent_type, int attack, int defense);

int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!");

   enumCount = DamageType
   for(int i = 0; i > enumCount)
   
   return 0;
}

int calculate_damage(const char *your_type, const char *opponent_type, int attack, int defense) {

}