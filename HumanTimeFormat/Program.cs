using System;
using System.Text;

namespace HumanTimeFormat
{
/*

    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
    public class HumanTimeFormat{
        public static string formatDuration(int seconds){

            // INITIALIZATION
            int yearDuration = 365;
            int dayDuration = 24;
            int hourDuration = 60;
            int minuteDuration = 60;

            // 
            StringBuilder date = new StringBuilder();

            int years = seconds/yearDuration;
            seconds = seconds%yearDuration;

            int days = seconds/dayDuration;
            seconds = seconds%dayDuration;

            int hours = seconds/hourDuration;
            seconds = seconds%hourDuration;

            int minutes = seconds/minuteDuration;
            seconds = seconds%minuteDuration;

            // STRING BUILDING
            //String strYear = new 

            // date.AppendFormat("{0} years, {1} minutes")
        }
    }
*/
}
